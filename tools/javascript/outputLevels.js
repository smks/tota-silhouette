const START_X = 0;
const END_X = 7;
const START_Y = -7;
const END_Y = 2;

for (let x = START_X; x <= END_X; x++) {
	for (let y = START_Y; y <= END_Y; y++) {
		console.log(`'${x},${y}',`)
	}	
}

// '0,-2'